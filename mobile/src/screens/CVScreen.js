// Deprecated duplicate CV screen.
// The canonical CV flow is implemented in CVUpload.js and registered as "CV".
// Keep this file as a compatibility shim for any legacy import.
export { default } from "./CVUpload";
